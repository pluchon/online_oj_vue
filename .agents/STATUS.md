# STATUS

> 现在谁在做什么、做到哪、卡在哪。**开工前先读这里，认领和收工都要改这里。**

最后更新：2026-09-25 · 由 `claude-code`

## 任务状态

一个任务一行，改的时候只动自己那一行，减少冲突。

| ID | 任务 | 状态 | 负责 | 认领时间 | 备注 |
|---|---|---|---|---|---|
| T-001 | 后端：标签表、关联表与 system 标签管理 | 已完成 | `claude-code` | 2026-09-25 | 用户 2026-09-25 验收通过 |
| T-002 | B 端：标签管理与题目抽屉选标签 | 已完成 | `claude-code` | 2026-09-25 | 用户 2026-09-25 验收通过 |
| T-003 | 后端 friend：按标签和做题状态筛选，删除关键词推断 | 已完成 | `claude-code` | 2026-09-25 | 用户 2026-09-25 验收通过 |
| T-004 | C 端：题库标签筛选和做题状态筛选 | 已完成 | `claude-code` | 2026-09-25 | 用户 2026-09-25 验收通过 |
| T-005 | AI 出题建议标签 | 已完成 | `claude-code` | 2026-09-25 | 用户 2026-09-25 验收通过 |
| T-006 | 后端：题解存储与读写接口 | 进行中 | `claude-code` | 2026-09-25 | 方案见 D-008 |
| T-007 | 后端：AI 生成题解草稿 | 进行中 | `claude-code` | 2026-09-25 | 依赖 T-006 |
| T-008 | B 端：题目抽屉题解页签 | 进行中 | `claude-code` | 2026-09-25 | 依赖 T-006、T-007 |
| T-009 | C 端：做题页题解页签 | 进行中 | `claude-code` | 2026-09-25 | 依赖 T-006 |
| T-010 | 后端：提交记录查询（friend 内部接口 + system client） | 待认领 |  |  |  |
| T-011 | 后端：按题重判 | 阻塞 |  |  | 依赖 T-010；等用户确认重判范围 |
| T-012 | B 端：提交管理页与重判 | 待认领 |  |  | 依赖 T-010、T-011 |
| T-013 | 后端：概览统计接口 | 待认领 |  |  | 依赖 T-010 |
| T-014 | B 端：数据概览首页 | 待认领 |  |  | 依赖 T-013 |
| T-015 | AI 补充功能（二选一） | 阻塞 |  |  | 等用户选方向 |

状态只用这五个：

- **待认领**：没人做，可以领
- **进行中**：有人在做，别碰
- **阻塞**：卡住了，原因写在下面
- **待验证**：做完了，等人验收
- **已完成**：验收通过

## 阻塞

- T-011：重判是否包含竞赛提交、是否重新结算排名，需要用户确认。
- T-015：赛后 AI 复盘和薄弱标签推荐二选一，需要用户决定。

## 交接记录

**最新的在最上面。** 写到下一个 agent 不用问你就能接着干。

### 2026-09-25 · `claude-code` · M1 第一轮验收反馈

- **做了**：
  - 已在本地 `oj-mysql` 执行增量 SQL（15 张表，18 个标签，8 条有效关联；重复执行结果不变）。
  - 标签选择改为「分类 + 标签」两段：B 端筛选栏、B 端题目抽屉、C 端题库筛选都用这种方式；只选分类、标签选全部时，按该分类下任一标签筛选（system 与 friend 新增 `tagCategory` 参数）。
  - 抽屉里的标签多选框右侧显示「已选 / 5」；已选标签不在当前分类里时，名称按 ID 从全部标签中取。
  - 标签管理弹窗：分类标题行最右侧显示「共 N 个标签」。
  - 修复：AI 出题输入框聚焦时是蓝色；B 端表格右侧固定列透明，横向滚动时底下内容透出来（全局给固定列设了不透明底色，用户、题目、竞赛三张表都生效）。
- **没做**：浏览器里没走查，由用户验收。
- **注意**：C 端题库也同步改成了分类 + 标签，用户验收时保留了这个做法。
- **收尾**：M1 由用户验收通过；后端提交 `0e8145e`，前端与 `.agents` 同一次提交。

### 2026-09-25 · `claude-code` · T-001 ~ T-005（M1 题目标签）

- **做了**：
  - 数据库：`oj_init.sql` 新增 `tb_tag`、`tb_question_tag` 两张表，初始 18 个标签，15 道测试题的标签也已配好；已有库用 `../online_oj/deploy/db_sql/upgrade/20260925_question_tag.sql` 升级。
  - system：`/system/tag` 增删改查；题目新增、编辑时带 `tagIds`（最多 5 个），列表支持 `tagId` 筛选，列表和详情都返回 `tags`；删除题目时同时删除标签关联；删除标签后通知 friend 刷新 ES。新增错误码 3303、3304。
  - friend：新增 `GET /friend/question/tags`；ES 文档加 `tagIds` 字段；题库检索改用 NativeQuery 构造 bool 查询，支持按 `tagId` 和 `userStatus` 筛选，MySQL 降级路径同样支持；删除 `resolveQuestionTags`；能力雷达图改为按标签分类统计。
  - oj_ai：AI 出题时从候选标签中挑 1~3 个建议标签，system 把名称换回 ID 再回填表单。
  - B 端：题目管理页加标签筛选、「标签管理」按钮与弹窗、标签列；题目抽屉可多选标签，AI 出题会回填建议标签；题目预览显示标签。
  - C 端：题库第二行加标签下拉框和做题状态分段（做题状态只对登录用户显示）；题目卡片在标题后显示标签；详情弹窗和做题页显示标签，竞赛答题中隐藏。
- **没做**：本地库没有执行增量 SQL，也没有重启服务联调；浏览器里没走查。
- **下一步**：由用户执行增量 SQL、重启 system / friend / ai，然后在浏览器验收；通过后把 T-001 ~ T-005 改为已完成，开始 M2（T-006 仍阻塞，需要先确认题解的存储方式和可见规则）。
- **注意**：
  - 已有的 ES 索引没有 `tagIds` 映射，首次写入时由动态映射生成 long 类型，功能上没问题。想用显式映射的话，删掉 `question` 索引后让 friend 重建。
  - 竞赛中隐藏标签只在前端处理，详情接口仍会返回标签。
  - C 端题库底部「未尝试」原来用列表总数减去全局已攻克和尝试中，加了筛选以后会算错，已改为用 `stats.totalCount`。

### 2026-09-24 · `claude-code` · 计划落档

- **做了**：检查了前后端现状，定下范围，写好 PLAN / STATUS / DECISIONS 和 AGENTS.md。
- **没做**：还没开始写任何代码。
- **下一步**：用户确认 PLAN.md「开工前要跟用户确认的」几项后，从 T-001 开始做。
- **注意**：现有标签由 `../online_oj/oj_modules/oj_friend/.../service/impl/QuestionServiceImpl.java` 的 `resolveQuestionTags` 按关键词推断，能力雷达图也依赖关键词匹配，T-003 要一起换掉。判题沙箱只支持 Java（`JudgeSandboxService` 里的 `javac Solution.java`）。
